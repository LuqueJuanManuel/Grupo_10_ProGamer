<!DOCTYPE html>
<html lang="en">
    <!-- head -->
<%- include("../partials/head", {title: "login"})  %>
   <!-- fin del head --> 

   <title>login</title>
<body>

    <!-- header -->
    <%- include("../partials/header",) %>
     <!-- fin del header -->

    <main id="login">
        <section>
            <div class="form-container">
                <h4 class="title">Iniciar sesión</h4>
                <!-- ingresando login -->
            <form action="/users/login" method="post">
                <div class="form">
                    <div class="input-container">
                        <input class="input-email" type="email" name="email" placeholder="Ingresar correo...">
                        <% if (locals.errors && errors.email) { %>
                            <small class="text-danger"><%= errors.email.msg %> </small>
                        <% } %>
                    </div>
                    <div class="input-container">
                        <input class="input-clave" type="password" name="pass" placeholder="Ingresar contraseña...">
                        <% if (locals.errors && errors.pass) { %>
                            <small class="text-danger"><%= errors.pass.msg %> </small>
                        <% } %>
                    </div>
                    <div class="checkUser-container">
                        <input class="input-check" type="checkbox" name="check" id="check" placeholder="Recordar usuario">
                        <label class="checkUser-label" for="check">Recordar usuario</label>
                    </div>
                    <div class="span-container">
                        <span> <a href="/users/register">Registrarme</a></span>
                        <span> <a href="">Olvide mi clave</a></span>
                    </div>
                    <div class="login-container">
                        <% if (locals.errorMsg ) { %>
                            <small class="text-danger"><%= errorMsg %> </small>
                        <% } %>
                        <button type="submit" class="btn-login" value="login">Login</button>
                    </div>
                    <h4>Iniciar sesión con....</h4>
                    <div >
                        <button type="submit" class="button-redes"><i class="fa-brands fa-facebook"></i> Iniciar con Facebook</button>
                    </div>
                    <div >
                        <button type="submit" class="button-redes"><i class="fa-brands fa-google"></i> Iniciar con Google</button>
                    </div>
                </div>
            </form> 
        </div>
        </section>
            
    </main>

   <!-- footer-->
      <%- include("../partials/footer")  %> 
   <!-- fin del footer -->
   <%- include("../partials/scripts") %>
</body>
</html>